did# TODO

- [x] Inspect existing routing/controllers for other sports (football/hockey/etc.) to mirror structure for golf
- [ ] Add Golf route to `routes/web.php` and add controller method in `BettingController.php`
- [ ] Create `resources/views/golf.blade.php` using the same “professional UI” pattern as `resources/views/football.blade.php`, but adapted for golf markets
- [ ] (Optional) Add golf-specific odds rendering and bet-slip wiring to use existing `/api/fixtures` and `/api/bet/*` endpoints with `sport=golf`
- [ ] Run a quick PHP syntax check / run tests (if any) and ensure no missing view/route errors


